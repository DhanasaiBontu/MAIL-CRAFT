{
  "name": "mailcraft-root",
  "private": true,
  "version": "1.0.0",

  "devDependencies": {
    "gh-pages": "^6.3.0"
  },
  "scripts": {
  "deploy": "npm run build --prefix frontend && gh-pages -d frontend/build -b gh-pages -r \"https://github.com/DhanasaiBontu/MAIL-CRAFT.git\""
}

}
